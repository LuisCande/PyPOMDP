- En el problema del Tag si está en la casilla 26 permite hacer las acciones North y West pero esto hace que el algoritmo tarde mucho en resolverlo

- El algoritmo pbvi que resuelve le problema del Tag no funciona, llega hasta el solve y no hace nada ni da error en la consola

- En caso de que no seamos capaces de solucionarlo, influye en nota si se resuelve SOLO con uno de los dos algoritmos (PBVI / POMCP)?

- Es estrictamente necesario implementar una interfaz de usuario? Vale permitir el tipo de simulacion por comandos en la consola?

- Que es el "discount" en los archivos .POMDP?

